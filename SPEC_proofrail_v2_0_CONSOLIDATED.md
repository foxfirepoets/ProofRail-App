# SPEC — ProofRail v2.4.0 (CONSOLIDATED — the only document the implementer reads)

```
Status: FROZEN for build | Date: 2026-07-04 | Merges: v1.0-v1.3 + Bridge Ops + SAG patches + Ultimate-Brainstorm adoptions (all superseded)
Upstream: ADP 2026-07-04 (READY\\\_FOR\\\_SPEC) + Cartography 2026-07-04
Law of the system:  COWORK = COGNITION · PROOFRAIL = PHYSICS · MCP = THE SEAM
                    DOCUMENTS = LAW (no EntityRegistry row → no fee, any stream, any entity)
                    NO PROOF → NO COMPLETION (fail closed, no bypass flag exists anywhere)
```

## 0\. Reading order \& current-truth manifest

Implementer reads, in order: (1) this spec, (2) `proofrail/CLAUDE.md`, (3) `proofrail/prisma/schema.prisma`, (4) `proofrail/mcp/tool-contracts.ts`, (5) `cowork-skills/\\\*/SKILL.md` (×4), (6) `COA\\\_\\\*\\\_v5.iif` + `Parent\\\*\\\_v5.iif` (×4 — v5 adds Ensign class 18 realm A wind-down, HLE class 16 parent held-for-sale), (7) `obgen/` (seeding CLI). **Anything versioned below v4/v2.0 is history, not instruction** (see Cartography supersession ledger). Everything validated: schema `prisma@6 validate` ✓ · contracts `tsc --strict` ✓ · IIF name-length CI ✓ · skill frontmatter ✓.

## 1\. What \& why

ProofRail = Adaptive-parity construction-finance app for STV on two QBO Advanced realms (A: 17 partnership Locations, B: 16 corporate classes incl. `15 STV CM`), plus the moat Adaptive lacks: SwarmSync **Invoice-Proof** (dup/line-math/BEC gate before the AP inbox), **Verify-API** (chain-hashed proof on every QBO write + every draw), **Audit-Proof** (nightly gate bundles). Operator: Ben, via Claude Cowork. Consumers: lenders, Ricks \& Co, litigation counsel (12SB/Union).

## 2\. Runtime architecture (from v1.2 — unchanged)

|Concern|Runtime|
|-|-|
|Email reading/classifying, PDF parsing, coding proposals, reply drafting, exception triage, Ben's console|**Cowork** (Gmail connector + ProofRail MCP + the 4 skills)|
|State machines, idempotency, proof gates, QBO writes, scheduled audits, money\_lock, fee engine, draw math|**ProofRail app**: Next.js 14 (Netlify) · NestJS ESM (Render) · Prisma 6 + Supabase Postgres (NEW isolated project) · Trigger.dev v4 · no Stripe, no RBAC|
|Single-writer law: Gmail is Cowork's; QBO is ProofRail's. Heartbeat (`heartbeat-watch`) alerts on intake silence without ever reading mail. Web UI ships **last** (P5) — Cowork IS the UI until the pipeline earns a dashboard.||

## 3\. Skill deployment map (where each skill fires — the ask of this spec)

|Skill|Surface|Fires when|Drives|
|-|-|-|-|
|**proofrail-operator**|Cowork recurring tasks + on-demand|Inbox Run (\~hourly 9–5 MT) · Approval Session (Ben-initiated) · Morning Brief (daily first)|Gmail actions + MCP: `lookup\\\_coding, submit\\\_intake, list\\\_queue, approve/reject, get\\\_gate\\\_status, reconcile\\\_draw\\\_sheet`|
|**proofrail-coding-rules**|inside Inbox Run step 3c + any coding question|every invoice coding decision|the (entity, project:phase, item) triple; fee-matrix awareness; member-vendor check; pre-gate suspicion notes|
|**proofrail-drawsheets**|inside Inbox Run step 4 + on-demand|every DRAW\_SHEET classification|format detect (AIA\_G703 / PROCORE\_PCI / LEGACY\_STACK) → canonical lines → six checks → `reconcile\\\_draw\\\_sheet`|
|**stv-oaea-registry**|on-demand + whenever an OAEA/amendment arrives in mail|registry births/changes|EntityRegistry row + diff + MemberVendor rows + cap-table deltas → shipped as migration|
|**Weekly Retro** (operator playbook 4)|Cowork recurring, Fri PM|weekly|volumes, proof pass rate, exceptions by cause, coding corrections made by Ben → propose 1–3 playbook/skill edits; write to memory ONLY after Ben confirms — the system's learning loop|
|**stv-monthly-close**|Cowork monthly cycle (bridge NOW; new-era list post-exit)|month-end + fixed-date calendar|Adam's 33 tasks: 13-step recon ×16 entities, packages, accruals, cash calls, payment calendar (human executes payments)|
|**Instrument layer (general skills the domain skills play through — install all):**||||
|Skill|Role in ProofRail|||
|---|---|||
|**gmail**|the hands of Inbox Run — scan/label/download/draft/send per operator's whitelist law; all Gmail actions route here|||
|**pdf-mastery**|every attachment: invoice extraction (F1), pay-app parsing feeding drawsheets adapters (F6), executed-OAEA reads for oaea-registry, draw-package PDF assembly (F3)|||
|**google-workspace**|Drive as the document estate: locate OAEAs/amendments (Operating Agreements folder), draw folders, Exhibits A; fetch for oaea-registry; file outputs back|||
|**google-sheets-mastermind**|financial workbooks: draw-analysis sheets (Union Walk Draw 13 Analysis pattern), CPA breakouts (AW1 P\&L pattern), lender-format tie-outs, GcCodeMap maintenance sheets|||
|Layering law: **domain skills decide, instrument skills execute.** Inbox Run = operator (orchestrates) → gmail (acts) → pdf-mastery (extracts) → coding-rules (judges) → MCP (submits). F6 = pdf-mastery/xlsx (parses) → drawsheets (translates + six checks) → MCP (reconciles). Registry = google-workspace (finds governing doc) → pdf-mastery (reads executed PDFs) → oaea-registry (extracts law → migration).||||
|Setup: install all **9** in Cowork's skills dir; connect Gmail connector + ProofRail MCP (`sk\\\_proofrail\\\_` key, SwarmSync key pattern). Skills carry zero authority — every rule they express is also a server-side guard (state machine, money\_lock, registry).||||

## 4\. Data model

`proofrail/prisma/schema.prisma` is normative (17 models, 10 enums). Highlights: enums ARE the state machines (`IntakeStatus` 11 states, `GateVerdict`, `DrawStatus`, `FeeStatus`, `EmailStatus/Class`, `FeeStream{DEV\\\_CM,DRAW,ACCOUNTING,PM}`); idempotency uniques `EmailItem.gmailMsgId`, `BillDraft.requestId`, `FeeRun@@unique(period,entity,stream)`; `EntityRegistry` carries the law (`feeBase` verbatim, `oaeaDocUrl`, `oaeaEffective`, `drawFee`, `acctFeeCapMo`, `pmFeeRate`); `GcCodeMap` (per-GC cost-code crosswalk); `MemberVendor` (double-pay trap); `DrawReconcile` (dual draw numbers, `retainageRate`, `format`); `ProofRef` (pointer only — SwarmSync ProofRecord is proof SoT); `McpAuditLog` (every mutation, before response). Registry rows born ONLY via stv-oaea-registry skill + migration. **Single-truth rule (SAG G-3): `obgen/config/entities.yaml` is legacy-extraction config only (qbw names, cutover date); any field describing current law (fees, capital, lenders) lives ONLY in EntityRegistry — never both.**

## 5\. MCP seam

`proofrail/mcp/tool-contracts.ts` is normative: `submit\\\_intake · list\\\_queue · approve · reject · get\\\_gate\\\_status · reconcile\\\_draw\\\_sheet · build\\\_draw · send\\\_draw · run\\\_fees · approve\\\_fees · lookup\\\_coding`. Three server-side laws: STATE GUARD (illegal transition → 409), MONEY LOCK (RED gate → 423 on send\_draw/approve\_fees), AUDIT (McpAuditLog before return). Error model PR-0xx per §8.

## 6\. Workflows (canonical)

**F1 AP intake:** email → Cowork parse+code → `submit\\\_intake` → server: Invoice-Proof (fail closed PR-003) → PASS→PENDING\_APPROVAL / FLAG→QUARANTINED → Ben approves (override\_reason required on FLAG, surfaces in nightly bundle) → QBO `POST /bill` (LocationRef+CustomerRef+ItemRef+ClassRef, RequestId) → Verify-API proof → PROOFED.
**F2 Nightly audit (Trigger.dev `nightly-gates`, 02:00 MT):** BS-by-Location both realms → gates **G-A** per-Location BS ties · **G-B** Unclassified=0 · **G-C** IC mirrors net 0 across realms · **G-D** zero forbidden-account postings · **G-E** fee compliance (all four streams vs registry) · **G-F** Arixa $317,137.06 present-labeled-unchanged → GREEN/RED → Audit-Proof bundle either way → RED sets `money\\\_lock`.
**F3 Draw build/send:** refuse if last GREEN >24h; assemble BvA from Items×Project; PDF carries chain\_hash; `send\\\_draw` requires PROOFED + confirm:true + no lock; SENT is irreversible.
**F4 Fee engine (four streams, all payee STV CM, LLC):** Commission sub-rule (evidence-resolved): commissions accrue per draw on the ASSESSED fee (gross, incl. contract-contribution fees), never on collections; the fee engine books commission accruals with the fee, settlement is a separate human-paid event. DEV\_CM 5%×entity `feeBase` riding draws · DRAW flat (`drawFee`, Madison $1,000) · ACCOUNTING (`monthly-accounting-fee` task, cap $500/mo) · PM (`pm-fee-on-GOI`, 1.0–1.5% GOI post-CO). Posting: Invoice (realm B, class 15 STV CM) + mirrored Bill (realm A) pair-atomic or compensating-void (PR-020). No registry row → refused.
**F5 Seeding (one-time, obgen):** EXTRACT runs inside the Rightworks-hosted desktop (QODBC binds only to the open .qbw there); build/emit/verify run anywhere. Extract per legacy file (CompanyName pre-flight) → mapping CSVs → gates G1–G4 → emit via QBO API to sandbox → G5 re-extract tie-out to the penny → Verify-API proof → CPA sign-off → live. Flagged balances → visible `OB Suspense - Flagged`, must read 0.00 pre-go-live.
**F6 Draw-sheet reconciliation (the leak detector):** format adapter (per proofrail-drawsheets) → GcCodeMap → six checks: fee math penny-exact (live catch: Madison Draw 6 billed $15,407.03 vs 5%=$15,307.03), retainage rate (Concord 5% / Elite 10%), dual numbering, COs billed⊆approved, line arithmetic, member-vendor scan → verdict + document proof. **Extraction humility law (brainstorm Q4 'dangerous-consensus' mitigation): every extracted line carries a confidence score; sub-high-confidence lines flag; for the first 60 days every F6 reconciliation gets mandatory human tie-out regardless of PASS** — extraction trust is earned per GC format, never assumed. Anomaly screen: invoice >2× vendor trailing average → WARN on the approval packet.
**MailOps (Cowork playbooks):** taxonomy INVOICE/DRAW\_SHEET/LIEN\_WAIVER/INSPECTION/LENDER\_DOC/BANK\_NOTICE/VENDOR\_INQUIRY/LENDER\_CORRESPONDENCE/**APPROVAL** (Mike/Zach approval replies — match to the pending intake/draw/fee and record as that gate's evidence)/OTHER; labels `ProofRail/{Invoices, Draws, Docs, Action, Quarantined, Risk-BankChange, Missing-Docs, W9-Insurance, Statements, Lender, Replied, DoNotPost, Processed, Archive}`; **Processed is terminal and applied ONLY after attachment save + Drive filing + log all succeed** (partial work never wears the done label); never delete; whitelist law (auto-send whitelisted only, proofed; drafts for all else; **money-content → never auto-reply, anyone**); attachments deduped (msg\_id, sha256). **Label→folder filing law (every download lands in BOTH places — app storage for the pipeline, Drive for humans, mirroring Adam's conventions):** INVOICE → `{entity}/Invoices/{YYYY-MM}/` · DRAW\_SHEET + LIEN\_WAIVER → the project's draws folder, next draw-number convention (`Draw N - {Lender} Draw #M/`) · BANK\_NOTICE/STATEMENT → the recon workbook's statements folder · LENDER\_DOC → the entity's Loans folder · LIEN\_WAIVER standalone → vendor doc tracking. Gmail label and Drive folder always correspond 1:1; a labeled email whose attachment isn't filed is an incomplete Inbox Run step. **Filename law (machine-parseable, sortable):** invoices `{YYYYMMDD}\\\_{ENTITY}\\\_{VENDOR}\\\_INV{no}\\\_{amount}.pdf` · draw pkgs `{PROJECT}\\\_DRAW{NN}\\\_{YYYYMMDD}\\\_{LENDER}.pdf` · statements `{YYYYMM}\\\_{ENTITY}\\\_{BANK}\\\_{last4}\\\_stmt.pdf`. **Do-Not-Post zone:** duplicates, superseded/voided docs, and examples file to `ProofRail/Do Not Post/` — a terminal hard-stop, never processed. **Historical Examples corpus:** `ProofRail/Historical Examples/` holds the labeled per-GC/per-vendor document corpus — it IS the fixture source for acceptance tests 2, 8, and 10 and for new-format adapter proving.

## 7\. Money / auth / proof boundaries

ProofRail NEVER creates BillPayment/transfer/charge — payment execution stays human-in-QBO. Auth: Gmail OAuth scopes = read + compose + labels, **never delete**; SwarmSync `sk\\\_` key rotated quarterly; MCP `sk\\\_` org key; approval requires key AND approvable state; QBO OAuth ×2 realms (refresh at 80% TTL, `qbo-token-refresh`); webhook receiver HMAC-verified; webhooks are hints, `qbo-reconcile-poll` is truth. Proof economics: irreversible actions (sends, QBO writes, reconciliations, bundles) → external proofs; reversible (labels, drafts, classifications) → local logs. Tiers: Invoice-Proof Verify (500/mo) · Verify-API Build (5,000/mo) · Audit-Proof Comply→Certify on first litigation export.

## 8\. Error codes (canonical set)

PR-001 parse×3→human · PR-002 FLAG approve w/o reason · PR-003 proof service down→fail closed · PR-010 QBO 429 backoff · PR-011 401 refresh-or-halt · PR-012 dup RequestId=success · PR-020 fee pair partial→void+FAILED · PR-030 money\_lock 423 · PR-040 low-confidence classification · PR-041 template fill fail→draft · PR-042 unparseable attachment · PR-043 unknown project/code→never guess · PR-044 Gmail quota→heartbeat. Plus STATE\_409, LOCKED\_423, NOT\_FOUND\_404.

## 9\. QBO dimensional law (both realms)

Location = legal entity (BS axis) · Class = cost phase · Customer:Job = project:phase · Items = cost codes. Every line classified; JEs zero per Location; IC Clearing must read 0.00 per class monthly; mirror pairs (`Due To Corp:X` ↔ `Due From Projects:X`) net zero across realms. Seed from COA v5 IIFs (18 partnership classes incl. 18 Ensign wind-down; 17 parent classes incl. 16 HLE held-for-sale + 15 STV CM; income 40200/40210/40220/40230). Wind-down law: Ensign books final-year activity + K-1 support in realm A, then class freezes; HLE carries Land Held for Sale at cost until disposition, then class freezes.

### 9.1 QBO integration runbook (the concrete Intuit-side steps)

1. developer.intuit.com → create app (scope `com.intuit.quickbooks.accounting`), capture client id/secret, set redirect URI to the Render API's `/auth/qbo/callback`. 2. Create TWO sandbox companies (realm A/B); record realm IDs in env (`QBO\\\_REALM\\\_A/B`); startup asserts realm-id-per-call (obgen pre-flight pattern). 3. In each company: Settings→Advanced→ enable **Location tracking** (label "Location") + **Class tracking, one per row**; enable Projects; create the COA v4 design via API — NOTE: QBO's API entity for Location is **`Department`** (UI label "Location"); create via `POST /department`, `POST /class`, `POST /item`, `POST /account` (IIF is Desktop-only; the v4 IIFs are the design source, not the import vehicle). 4. OAuth flow once per realm; store refresh tokens encrypted; `qbo-token-refresh` at 80% TTL. 5. Pin `minorversion` in every call; bump deliberately. 6. Register webhook endpoint (`/webhooks/qbo`, HMAC verifier token) for Bill/Invoice/JournalEntry change events — hints only; `qbo-reconcile-poll` is truth. 7. Throttle budget ≤50 req/min/realm (10% of the \~500 limit); honor Retry-After on 429. 8. Sync path per bill: `POST /v3/company/{realm}/bill?minorversion=NN` with LocationRef+ClassRef+CustomerRef+ItemBasedExpenseLine + RequestId header. 9. Reports: BalanceSheet with `summarize\\\_column\\\_by=Location` feeds gates G-A/G-C. 10. Go-live = repeat 2–4 against production realms after CPA sign-off (P6). 11. Connect bank feeds for all feed-capable accounts (manual-entry stays manual: STDG MACU/Granite, STVE per skill). 12. Invite Ricks \& Co as accountant users on both realms (read access = bus-factor mitigation, ADP §15).

### 9.15 Intuit-AI coexistence policy (both realms)

Intuit Intelligence ships inside QBO; ProofRail neither fights nor trusts it. **ON:** bank-feed categorization suggestions, AI reconciliation assist (statement-vs-account diff), anomaly/trend insights, AI chat. **OFF / supervised:** Project Management Agent auto-creation (generic AI inventing Customer:Jobs violates dimensional law), auto-post rules beyond payroll/BillPay matches. **Law:** Intuit-AI output = hints; ProofRail gates = truth. AUTO-badged postings reviewed at monthly close; gates G-A/G-B audit Intuit's AI drift nightly like any other writer. Nothing Intuit posts bypasses proof requirements — its writes are Ben-side writes.

### 9.2 Environment manifest (the hour-one contract)

Render API: `DATABASE\\\_URL` `DIRECT\\\_URL` (Supabase) · `QBO\\\_CLIENT\\\_ID` `QBO\\\_CLIENT\\\_SECRET` `QBO\\\_REALM\\\_A` `QBO\\\_REALM\\\_B` `QBO\\\_REDIRECT\\\_URI` `QBO\\\_WEBHOOK\\\_VERIFIER` `QBO\\\_MINORVERSION` · `SWARMSYNC\\\_API\\\_KEY` (STV org, rotate quarterly) `SWARMSYNC\\\_BASE\\\_URL` · `PROOFRAIL\\\_MCP\\\_KEY` (sk\_, Cowork's credential) · `TRIGGER\\\_SECRET\\\_KEY` · `SMTP\\\_URL` (draw sends). Netlify web: `NEXT\\\_PUBLIC\\\_API\\\_URL` only. Secrets in platform vaults, never repo; sandbox and production sets fully disjoint.

## 10\. Build plan (phases, each with validation + binary acceptance)

**P0 — BRIDGE OPERATIONS (starts NOW, parallel to all phases — the July close and every month until exit):** **Ledger-of-record during bridge = the legacy QB Desktop files on Rightworks** (the 2 QBO realms hold nothing real until P6 seeding); cutover TB date = the last bridge-closed month-end, so G9 slip is absorbed by design, never by drift. Cowork runs the full monthly cycle via the **stv-monthly-close** skill (Adam Ludvigson's 33 recurring tasks, translated: 13-step recon per entity ×\~16, close tasks, financial packages by the 15th, cash calls, payment calendar with funds-verification — payments always human-executed). ✔ July close-tracker: all entities green; Week-1 intake volume measured and reported (invoices/wk, draws/wk — the Quantifier baseline); packets emailed to Mike per LLC; zero payments executed by Cowork. **Bridge exit test:** one full month where every ABSORBED task ran through ProofRail with proofs and the close packet tied without manual correction.
**P1 — Provisioning (wk1):** SwarmSync STV org + entitlements · Supabase project + `prisma migrate dev` · QBO sandbox ×2 + OAuth · run **stv-oaea-registry** against all OAEAs (12 unverified entities → rows or explicit NO\_FEE/flag). ✔ `npx prisma migrate status` clean; ✔ registry rows for all 17 entities each citing `oaeaDocUrl`; ✔ `POST /api/verify` returns proof\_id under STV org.
**P2 — API core + MCP (wk1–2):** intake/proofgate/approve/sync modules + MCP module (the seam ships FIRST) + shared `gates` package (obgen imports it — kills the ADP §12 duplication). Test-first: state-transition table (illegal→409) · money\_lock middleware (RED→423 on exactly send\_draw+approve\_fees) · registry-sourced fees (no row→refused, all 4 streams; 12SB land-inclusive base→refused) · submit\_intake idempotency (×3→1) · fee pair atomicity (mock A-fail→B void→FAILED). ✔ all green in CI.
**P3 — Cowork playbooks + seeding (wk2–3):** install 4 skills; Inbox Run in **draft-only** mode; obgen `--target qbo-sandbox` → G5 tie-out 0.00 all entities, proof-recorded. ✔ acceptance #1,2,6,7,8,11 below.
**P4 — Gates + money\_lock (wk3):** Trigger.dev `nightly-gates`+Audit-Proof+`heartbeat-watch`+`qbo-token-refresh`+`proof-replay-queue`+`qbo-reconcile-poll`. ✔ #3,12,13.
**P5 — Web UI + Draws + F6 + Fees (wk4):** Adaptive-parity screens (`/ap /projects/:id/budget /draws /vendors` + entity switcher) · Draw Builder · F6 adapters + GcCodeMap loader (Concord 109 codes, Elite items+CSI) · fee engine 4 streams (`monthly-fees`, `monthly-accounting-fee`, `pm-fee-on-GOI`). ✔ #4,5,9,10.
**P6 — Live cutover:** CPA sign-off → live seeding → 30-day 100%-human approval → auto-send after 2 clean draft weeks → sampling per open item.

## 11\. Acceptance suite (all binary; the DoD)

1 Sandbox: F1–F6 end-to-end, every proof URL verifies. 2 20 historical invoices replayed: coding ≥95%, planted dup caught, planted BEC caught. 3 Nightly GREEN 7 consecutive days live; injected RED provably 423s fee+draw. 4 Fee run posts mirrored pair; IC nets 0.00; **fee for entity without registry row refused with logged reason (all four streams)**. 5 Draw PDF carries chain\_hash; Ben validates vs one historical Arixa package. 6 obgen G5 delta 0.00 all entities, tie-out proofed. 7 Every QBO mutation in 24h test window has a ProofRef (count match). 8 50 historical emails: classification ≥95%, zero off-whitelist auto-sends, zero deletes. 9 Planted whitelisted BEC email quarantined, not replied. 10 Historical Rich draw sheet through F6 → known markup line flags; **Madison Draw 6 through F6 → the $100 CM-fee variance flags**. 11 Same email ×3 → one EmailItem/route/reply. 12 Kill Cowork 24h → gates still run+bundle, money\_lock enforceable. 13 MCP approve on PROOFING intake → 409. 14 Full loop: real email → Inbox Run → proof PASS → approve in chat → bill in sandbox QBO → proof verifies — one conversation, zero terminal.

## 12.5 Recurring operations calendar — the new-era monthly cycle (what survives Adam's list)

Daily (Morning Brief adds): cash position from wire/balance intel · gate verdict · queue/quarantine. Trigger.dev (new tasks beyond §2): **`monthly-accruals`** (property-tax JEs — expense operating / capitalize development; internal-loan interest JEs) — added to the task roster. Monthly, Cowork (stv-monthly-close §5 REMAINS list): M1 close packet per entity (BS/P\&L by Location + BvA + T-12, Audit-Proof close bundle attached) · M2 formal bank rec per entity (feeds gate G-A; manual-entry banks stay manual) · M3 PM T-12 ingestion (Cornerstone/NXT) · M4 loan/lender statement tie + retainage · M5 cash-flow forecast refresh · M6 registry drift check (OAEA Update Ledger vs EntityRegistry rows) · M7 payment-calendar funds verification (fixed dates per skill §4) · M8 proof-tier usage + McpAuditLog review · M9 GcCodeMap/vendor-alias hygiene · M10 cash-call cycle (\~9th–16th; notices drafted, Ben releases). Biweekly: STVE payroll funds check. The nightly gates absorb daily integrity; the monthly layer produces the human-grade artifacts (packets, forecasts, calls) that gates cannot.

## 12\. Open verifications (registry work, non-blocking — engine refuses until resolved)

Per-entity fee-clause verbatim reads: HLN, Union, Quincy, Ledges, Freeman, Ventura, Vic, Ensign, Elephant Rock, RM Texas (template presumed, base required) · Carlo/EJH/Dominus/Camden fee status · STV CM's own OA (do CEO 2%/Pres 1% ride inside it?) · Union Walk GC format (open one package; PROCORE\_PCI presumed) · Audit-Proof tier upgrade trigger = first litigation export · post-30-day approval sampling rate (proposal: 20% + 100% >$10K + 100% previously-overridden vendors) · ~~HLE / Ensign homes~~ **RESOLVED 2026-07-04:** HLE = parent class 16 (held for sale, Land Held for Sale 13900); Ensign = partnership class 18 (wind-down, Lazarus capital sub + parent mirrors) · **12SB fee retroactivity** (engine posts prospective-only until counsel+CPA rule) · **Intuit QBO MCP** — RESOLVED to policy: available now (intuit/quickbooks-online-mcp-server, MIT, local Node, full CRUD + DISABLE flags; community laf-rge/quickbooks-mcp adds BS/PL/TB-by-class report tools). Use = DEV-TIME ONLY, Claude Code against sandbox realms, DISABLE\_DELETE=true — a P1/P2 accelerant for seeding + G5 verification. NEVER connected to Cowork or production: a raw QBO MCP is a side door around the state machine, proof gate, and money\_lock (single-writer law). ProofRail MCP remains the only runtime door · ~~CM-fee commission base~~ **RESOLVED 2026-07-04 (evidence: Adam's Development Fee Worksheet, penny-verified across 4 projects):** commission = % × the ASSESSED 5% fee, **GROSS, accrual basis** — accrues at assessment (work-submitted date), includes contract-contribution fees, independent of cash collection; settled via periodic payments. Historical practice: Zach 3.00% of fee every row since 6/2024 (column header says 2% — stale); Porter 1% column = $0.00 forever · ~~Mike approval channel~~ **RESOLVED: email is Mike's channel for everything** — APPROVAL email class + reply-approval as gate evidence is the permanent mechanism; approval packets and financial packages go TO Mike by email, never a login · **Commission rate/recipient forward structure** (the one remaining confirm): worksheet practice = Zach 3% of assessed fee; parent COA reserves Watson 2%/Christensen 1%; reconcile all three against STV CM's own OA before ProofRail's first commission run — base is settled, rates/recipients are not.

