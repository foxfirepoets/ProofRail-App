# Progress Log (append-only)

Project: ai-accounting-hub
Initialized: 2026-06-26
Total chunks: 8

## Log

(no entries yet)
[2026-06-26] CHUNK_1_INFRA task: scaffold canonical store + app spine + init migration + tests — DONE (local gate GREEN: ruff/mypy/pytest 12 passed, 2 integration skipped). Live DB migration owner-gated (Supabase egress).
[2026-06-26] Wave 1 CHUNK_2_TRANSPORT (24 tests) ‖ CHUNK_4_AUDIT (23 tests) — built in parallel, isolated packages. Integrated: router wired into main.py, cryptography added to requirements. Full gate GREEN: ruff/mypy/pytest 59 passed, 2 integration skipped.
[2026-06-26] Wave 2 CHUNK_3_CANONICAL (20+1 tests) — unified cross-company search (pg_trgm) + read API + dashboard, enveloped 400/404. Router wired. LIVE DB RESOLVED: migration 20260626_1200 applied to Supabase fdnwlcomuddzmluvbylg via Management API; .env DATABASE_URL switched to verified aws-1-us-west-2 session pooler; full suite 80 passed/0 skipped vs LIVE DB.
[2026-06-26] Wave 3 CHUNK_5_WORKFLOW (23+2 tests) — NATS+Temporal seams w/ in-memory fakes, ATXN capability 403, AIVS fail-closed commit gate, idempotency. Router wired; nats-py+temporalio added to requirements. Full gate green.
[2026-06-26] Wave 4 CHUNK_6_VERIFY (42 verify+transport tests) — VerifyAPI Gate 3 (in-process, fail-closed, independent_attestor_signature), gated qbXML BillAdd write-back with EditSequence re-base (QB_EDIT_CONFLICT 409) + CoA_DRIFT 422 pre-check. Transport extended add-only, tests green. Full suite 120 passed/5 skipped. Workflow→verify wiring deferred to CHUNK_8 e2e.
[2026-06-26] Wave 5 CHUNK_7_PAYMENTS (28+1 tests, ATOMIC) — InvoiceProof Gate 1 (all 7 rules), ATEP bank-change Gate 4 (fingerprints only, no raw bank), invoice2data OCR, VCAP Full Bundle (HMAC) + AuditProof, atomic CAS release (40-thread proven). Fail-closed proven. Router wired; invoice2data+setuptools<81 added. Full gate green.
[2026-06-26] Wave 6 CHUNK_8_SCALE (12 tests) — 10-company sync orchestrator, QBOAdapter seam stub (~0% caller delta), GET /sync/companies, end-to-end pipeline (OCR->Gate1->approval->AuditProof->VerifyAPI->BillAdd) green with REAL proof logic, fail-closed-under-outage proven at all 3 gates. BUILD COMPLETE: 8/8 chunks. Full suite 160 passed/6 skipped; vs LIVE Supabase 165 passed/1 skipped.

## 2026-06-28T14:29Z — DASHBOARD: Accounting Work Queue / Operator Dashboard (FinalSpec Phase 1)
- Built app/dashboard/ (router+service+11 Jinja2 templates), mounted /ui. Server-rendered, read-mostly.
- Pages: home, companies, vendors, bills, draws (+detail), draw review, sync health. Persistent SHADOW banner on every page.
- Actions (approve/reject/mark-historical/resolve-candidate/remap-cost-code) transition canonical status/mappings ONLY.
- Shadow guard: not_for_posting/historical draw can never be approved-to-post (Draw #29).
- Fee panel reuses pure fee_math 5/2/1 + anti-13% (8% distinct). jinja2 added to requirements+AGENTS.
- Gate: ruff+mypy(74)+pytest 250 passed/47 skipped; live render proof 6 pages 200+banner.

[2026-06-28] Accounting Work Queue / Operator Dashboard (DASH-1..7) PASSED TBL audit — GREEN_COMPLETE (commit 3fa6325). 250 passed/47 skipped; shadow-safe (AST no-QB-path test); Draw #29 guard wired into /ui/draws/{id}/approve; FinalSpec Phase 1 sync panels render.

[2026-06-28] AWQ-1..3 Accounting Work Queue: modular 15-module registry (Draw Review functional + 14 pending placeholders), grouped left-nav, /ui work-queue landing, /ui/queue/{key} placeholder route. 269 passed/47 skipped. Shadow-safe (AST test still green); no QB/BillAdd/payment path added; Draw Review + not_for_posting guard intact.

[2026-06-28] Accounting Work Queue expansion (AWQ-1..3) PASSED TBL audit — GREEN_COMPLETE (commit c3047c6). 15-module registry, work-queue landing + grouped nav, 14 pending placeholders; Draw Review non-regressed + guard intact; AST no-QB-path test passes; 269 passed/47 skipped.

[2026-06-28] VB-1..4 Vendor Bills / Non-GC Invoices module ACTIVATED (functional). app/dashboard/vendor_bills.py: intake+vendor-match (else queue candidate), invoice/amount/due-date capture, duplicate-invoice exception, vendor bank-change warning (SHA-256 fingerprint only — never raw), missing-coding exception, approve/reject/needs-info canonical-status-only actions each writing an AIVS audit row. Reused Bill.raw_extensions (no schema change). Routes /ui/vendor-bills (list+intake), /ui/vendor-bills/{id}, POST intake + actions. modules.py vendor_bills -> functional. Gate: ruff+mypy ok, 280 passed/48 skipped; live flow + render proof rolled back. Shadow: AST no-QB-path test covers vendor_bills; no QB/BillAdd/payment path.

[2026-06-28] Vendor Bills / Non-GC Invoices module (VB-1..4) PASSED TBL audit — GREEN_COMPLETE (commit 9f0f779). Intake+extraction, vendor match, duplicate detection, bank-change FINGERPRINT-only warning, missing-coding exception, approve/reject/needs-info canonical-only + audit trail per action; module now functional in work queue; Draw Review intact; 280 passed/48 skipped. Advisory: banner not exercised on /ui/vendor-bills in an offline unit test (renders via Jinja global).

[2026-06-28] FIN-1 Remaining modules functional (shadow mode) — generic WorkItem work-queue framework. Added ONE canonical model WorkItem (table work_items: company_id FK CASCADE, module_key, title, reference, counterparty, amount Numeric(14,2), txn_date, status default needs_review, project_ref/customer_job/class_ref/item_cost_code, bank_fingerprint [SHA-256 ONLY], raw_extensions JSONB). Migration 20260701_1000 (down_revision 20260630_1000) with real downgrade; APPLIED to Supabase fdnwlcomuddzmluvbylg — new head 20260701_1000, work_items table live. New engine app/dashboard/work_queue.py: intake_work_item / list_work_items / get_work_item / set_work_item_status (approve→approved_for_accounting, reject→rejected, needs-info→needs_info) / work_item_count + exceptions MISSING_CODING, DUPLICATE, BANK_CHANGE warning (fingerprint reused from vendor_bills.bank_fingerprint — never raw); every intake + action writes an AIVS audit row tool_name="work_queue:{module_key}". 11 WorkItem-backed modules (non_gc_invoices, bank_feed, credit_card, loan_draws, interest_reserve, owner_contributions, distributions, intercompany, developer_fees, management_fees, vendor_setup) functional with routes GET /ui/m/{key}, GET /ui/m/{key}/{id}, POST /ui/m/{key}/{id}/{approve|reject|needs-info}, POST /ui/m/{key}/intake (JSON or form). bank_feed/credit_card/vendor_setup use fingerprint-only bank handling. 2 read-only aggregation views month_end + missing_dimensions at /ui/m/month_end and /ui/m/missing_dimensions (aggregate draws + vendor bills + work items). Templates work_queue_list/work_queue_detail/month_end/missing_dimensions extend base.html (SHADOW banner renders). modules.py: all flipped functional; added single pending module qb_sync "QuickBooks Sync / Write-back" (group QuickBooks) as the ONLY remaining placeholder. service.work_queue_overview gives live counts for every module. Gate GREEN: ruff + mypy(77) + pytest 291 passed/49 skipped. Live proof (RUN_INTEGRATION=1, rolled back): intake in bank_feed+distributions appears with live count; DUPLICATE, MISSING_CODING, BANK_CHANGE each fire; no raw bank string stored (fingerprint only); approve changes status + writes audit row; month_end + missing_dimensions surface seeded items; SHADOW banner on all new pages. Shadow-safe: package AST no-QB-path test now covers work_queue.py + dedicated AST test; no transport/qbwc/draw_engine/payments import, no BillAdd/add_bill/process_draw/execute_payment call. Draw Review (Draw #29 not_for_posting guard) + Vendor Bills intact.

[2026-06-29] FIN-1 (12 remaining modules functional) VERIFIED GREEN (commit 518128c, Thon build). Gate: ruff+mypy(77)+pytest 291 passed/49 skipped. Only qb_sync pending; WorkItem fingerprint-only; shadow-safe (AST covers work_queue.py); no secrets. Verified directly post session-reset.

[2026-06-29] FIN-2 Cross-entity search + "What needs my attention today" (read-only, shadow mode — NO migration). New app/dashboard/search.py: parameterized SQLAlchemy ILIKE/trigram search (pg_trgm GIN on vendors.name) across draws (draw_packages), GC bills + vendor bills (split by raw_extensions.bill_type), vendors, invoices/work-items (work_items), projects (draw customer_job + work-item project_ref/customer_job), and open exceptions (reusing work_queue.month_end_exceptions); normalize_query → empty/short q renders a friendly prompt (200), never an error; results grouped by entity type, each hit linking to its existing detail page (/ui/draws|bills|vendors|vendor-bills|m/{key}). service.attention_overview aggregates everything needing action across ALL modules into 5 urgency-typed groups (Open exceptions / Vendor bank-change warnings / Missing coding / Needs info-review / Pending approvals) with counts + links, reusing work_queue.month_end_exceptions + missing_dimensions_items rollups (no duplicated logic); bank-change derived from fingerprint warnings only (raw bank fields never read). Routes GET /ui/search?q= and GET /ui/attention (router.py); templates search.html + attention.html extend base.html (SHADOW banner). Search box added to base.html nav (reachable every page) + "⚑ What Needs My Attention" link; attention summary panel folded into work_queue_overview and rendered as a prominent panel on /ui home (gracefully absent when overview stub omits it). Gate GREEN: ruff clean + mypy(78) + pytest 298 passed/50 skipped. Live proof (RUN_INTEGRATION=1, seeded-in-txn then ROLLED BACK): search 'Hunters' → 4 hits across Draws/Work Items/Projects/Exceptions with real detail links; search 'Acme' → Vendor Bills/Vendors/Exceptions; /ui/attention total 9 (Open exceptions 3, bank-change 1, missing-coding 2, needs-review 3, pending 0) with real links; SHADOW banner rendered on both /ui/search + /ui/attention live responses. Read-only: no new DB writes, no migration, no QB/QBWC/BillAdd/payment path (package AST shadow-safety test + dedicated search.py AST test both pass). No secrets/.env committed.

[2026-06-29] FIN-2 (cross-entity search + attention dashboard) VERIFIED GREEN (commit ec6bb01, Thon). /ui/search + /ui/attention register; read-only, shadow-clean; gate 298 passed/50 skipped; no secrets.
