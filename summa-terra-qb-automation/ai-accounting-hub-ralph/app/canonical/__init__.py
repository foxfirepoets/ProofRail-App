"""CHUNK_3_CANONICAL: read layer + unified cross-company search over the canonical store.

The canonical Postgres (Supabase) store is the system of record; this package is a
read-only view over it. Nothing here mutates the books.
"""
