# WEEKLY RETRO — paste Friday PM

Run the weekly retro (the system's learning loop):

1. From this week's `logs/cowork_audit_*.jsonl` + `logs/qbo_seed_*.jsonl`, compute:
   volumes (emails by class, invoices, draws) · InvoiceProof pass/flag/fail rates ·
   sandbox postings (count, total $, realms) · exceptions opened/cleared (by cause) ·
   Ben's coding corrections (every place his final coding differed from your proposal) ·
   drafts created vs sent · anything that aged past SLA.
2. Top 3 friction points, each with the specific evidence (cite events/msgids).
3. Propose 1–3 playbook/spec edits (exact file + exact wording change) that would have
   prevented this week's corrections or friction. Keep them surgical.
4. **Adopt NOTHING yourself.** Present the proposals; only after Ben replies "adopt #N" do you
   edit the file, and then log the change (`retro_note` event with before/after).
5. Close with next week's watch items and any unresolved owner questions (commission ruling,
   unverified OAEA bases, whitelist additions).
